import React from 'react';

// This component is intentionally empty as its content has been redistributed
// to AssetShowcase (Grid of Cards) and Calculator (Roadmap) to better match
// the "Stable" reference design layout which doesn't have a distinct testimonials carousel.
export const OutcomeStories: React.FC = () => {
  return null;
};